version: '3'

services:
  producer:
    build:
      context: ./producer
      dockerfile: Dockerfile
    command: npm start
    ports: 
      - "3000:3000"
    volumes:
      - ./producer:/usr/src/app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
